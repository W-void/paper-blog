---
title: "大模型时代，研发还有未来吗？"
date: 2026-06-10
tags: [公众号, AI, 职业]
---

export const AutoIframe = ({src, title}) => {
  if (typeof window === 'undefined') return null;
  return (
    <iframe
      src={src}
      title={title}
      onLoad={(e) => {
        try {
          const doc = e.target.contentDocument || e.target.contentWindow.document;
          const height = doc.documentElement.scrollHeight;
          e.target.style.height = height + 'px';
        } catch (_) {}
      }}
      style={{
        width: '100%',
        height: '3000px',
        border: 'none',
        display: 'block',
      }}
    />
  );
};

<AutoIframe
  src="/paper-blog/wechat-html/mp_article_ai_dev.html"
  title="大模型时代，研发还有未来吗？"
/>
